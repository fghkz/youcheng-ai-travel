# 待修改问题

> 本文档用于记录已确认、尚未实施的产品与代码问题。只有在用户明确提出统一修改时才处理。

## 1. 行程天数与用户选择的日期范围不一致

- 状态：已完成（2026-08-15）
- 发现位置：行程结果标题与“行程天数”统计
- 现象：用户选择多日行程（例如 5 天），页面可能仍显示“1 日漫游计划”。
- 触发条件：DeepSeek 只返回有景点安排的日期，没有返回日期范围内的全部日期。
- 根因：
  - 当前标题和统计使用 `result.itinerary.days.length` 作为行程天数。
  - 服务端只校验模型返回的日期是否位于用户选择范围内，没有校验日期是否完整。
  - DeepSeek 提示词没有明确要求为日期范围内的每一天返回一个 `ItineraryDay`。
- 预期行为：
  - 行程天数始终根据开始日期和结束日期（含首尾两天）计算。
  - 返回结果必须覆盖日期范围内的每一天。
  - 没有景点安排的日期仍需展示，并标记为“自由活动”或“当天暂无景点安排，可继续添加景点”。
- 建议修改：
  1. 提示词要求 DeepSeek 返回日期范围内的全部日期，包括空行程日。
  2. 服务端校验日期集合必须与用户选择的完整日期集合一致。
  3. 服务端在模型遗漏日期时自动补齐空行程日，避免完全依赖模型输出。
  4. 页面标题和行程天数统计改为根据用户选择的日期范围计算，不再依赖 `days.length`。
  5. 增加单日、多日、景点少于天数及模型漏日的测试用例。

## 2. 删除“数据状态”并新增“交通路线”区域

- 状态：已完成（2026-08-15）
- 发现位置：行程结果右侧栏
- 修改要求：
  1. 删除现有“数据状态”卡片，不再向用户展示“景点信息 Live”“交通路线 Live”“行程规划 Live”等技术状态。
  2. 在原区域新增独立的“交通路线”卡片。
  3. “交通路线”卡片提供“查看地图”入口，点击后展示高德地图路线。
- 地图展示要求：
  - 按行程日期切换或分组展示每日路线。
  - 标出当天景点位置、游玩顺序以及景点之间的路线连接。
  - 明确区分公交与自驾路线，并展示交通方式、预计耗时、距离和路线摘要。
  - 只使用景点坐标及高德 API 返回的真实路线数据，不允许 DeepSeek 生成、猜测或修改路线事实。
  - 路线不可达、无结果、接口超时或限流时展示明确提示，不绘制虚假路线。
- 交互建议：
  - 桌面端使用大地图弹窗或独立地图面板，移动端使用接近全屏的地图界面。
  - 多日行程提供“第 1 天 / 第 2 天”等切换入口，默认展示第一天。
  - 支持地图缩放、拖动、景点标记点击、关闭、返回和失败重试。
- 实现注意事项：
  - 检查当前高德路线接口是否保留了绘制路线所需的步骤坐标或轨迹数据；如目前仅保存耗时、距离和摘要，需要扩展服务端路线模型及返回字段。
  - 高德 JS API Key 与安全密钥继续通过环境变量配置，禁止在仓库中写死或提交真实密钥。
- 统一修改前需要确认：
  - 地图采用弹窗展示，还是直接嵌入行程结果区域。
  - 多日路线按天切换展示，还是额外提供全部日期的路线总览。
