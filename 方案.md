# Supabase 用户系统与数据库实施方案

## 实施范围

本次创建 6 张自建表，并复用 Supabase Auth 的 `auth.users`：

1. `public.profiles`：用户资料。
2. `public.trips`：旅行项目和规划条件。
3. `public.trip_spots`：已选景点及供应商数据快照。
4. `public.itinerary_versions`：AI 行程历史版本与当前版本。
5. `public.favorite_spots`：用户收藏。
6. `private.generation_runs`：服务端 AI 调用遥测。

首版不创建分享、协作、支付、通知和评论表，也不拆分每日行程、行程项和路线段。行程结果以经过应用层校验的 `jsonb` 整体保存。

## 数据关系

- 一个 Auth 用户对应一个 `profile`。
- 一个用户可以拥有多个 `trip` 和多个 `favorite_spot`。
- 一个 `trip` 最多包含 8 个 `trip_spot`。
- 一个 `trip` 可以保留多个 `itinerary_version`，但最多一个是当前版本。
- 删除用户会级联删除资料、行程和收藏；删除行程会级联删除已选景点与行程版本，但不会删除收藏。
- `generation_runs` 删除用户或行程后保留匿名化统计记录，对应外键设为 `null`。

## 安全与一致性

- 所有表启用 RLS。
- `authenticated` 只能访问 `auth.uid()` 与 `user_id` 一致的数据。
- 子表使用 `(trip_id, user_id)` 复合外键，阻止伪造行程归属。
- `private.generation_runs` 不向 `anon` 或 `authenticated` 授权。
- 注册后由最小化触发器自动建立 `profiles` 记录。
- `updated_at` 由数据库触发器维护。
- 创建新行程版本必须调用事务型 RPC，先取消旧当前版本，再写入新版本。
- 所有枚举范围、日期区间、每日时间、坐标、JSON 对象和字符串长度均由数据库约束兜底。

## 文件与执行顺序

1. 运行 `supabase/migrations/20260815193440_create_travel_product_schema.sql`。
2. 运行 `supabase/migrations/20260815193628_add_trip_owner_fk_indexes.sql`。
3. 运行 `supabase/tests/database/travel_product_schema.test.sql`。
4. 执行 Supabase Database Linter / Advisors。
5. 通过后再接入 Next.js Auth、保存行程、历史版本和收藏界面。

具体字段、类型、默认值与约束以迁移 SQL 为唯一执行来源；本地和远程操作方法见 `supabase/README.md`。
