# AI 旅行规划网站产品需求文档

| 项目 | 内容 |
| --- | --- |
| 产品名称 | AI 旅行规划网站 |
| 文档版本 | V1.0 |
| 产品阶段 | MVP |
| 文档状态 | 可进入设计与开发 |
| 目标平台 | 响应式 Web（桌面端与移动端） |
| 产品语言 | 简体中文 |

## 1. 文档目的

本文档用于明确 AI 旅行规划网站 MVP 的产品范围、用户流程、功能要求、业务规则、数据约定和验收标准，供产品、设计、前端、后端及测试人员共同使用。

## 2. 产品概述

### 2.1 产品背景

自由行用户在制定行程时，通常需要分别查询景点、开放时间、票价以及景点之间的交通路线，再手动判断每天能否按时完成。信息分散且路线组合复杂，容易出现时间冲突、行程过满或路线不可执行等问题。

本产品通过聚合景点数据、地图路线数据与大模型规划能力，让用户先选择真正想去的景点，再生成时间合理、路线可执行的按日行程。

### 2.2 产品目标

1. 帮助用户根据目的地快速发现并选择景点。
2. 结合游玩日期、每日可用时间、景点信息及真实路线耗时生成可执行行程。
3. 清晰展示每日景点顺序、时间安排、交通方式、路线耗时、开放时间和参考票价。
4. 支持用户增删景点并重新生成行程。
5. 对缺失或不可靠的第三方数据进行明确标注，不使用 AI 伪造开放时间、票价或路线数据。

### 2.3 核心价值

- 先选景点再规划，结果更符合用户真实偏好。
- 将交通耗时纳入安排，降低“看起来合理、实际走不通”的风险。
- 用户调整景点后可快速重新规划，减少反复手工排程。
- 明确区分第三方事实数据与 AI 规划建议，提高信息可信度。

### 2.4 目标用户

- 需要制定单城市自由行计划的个人或同行小组。
- 对目的地不熟悉，希望快速获得初步行程的游客。
- 已有部分目标景点，希望优化游玩顺序和交通路线的游客。

### 2.5 典型使用场景

- 用户计划周末前往某城市，希望在每天 09:00—18:00 内安排多个景点。
- 用户已确定若干必去景点，希望比较公交和自驾后获得更高效的游玩顺序。
- 用户看到生成结果后临时增加或删除景点，需要重新生成可执行行程。

### 2.6 MVP 不包含的范围

- 用户注册、登录及账号体系。
- 数据库、云端保存、历史行程和跨设备同步。
- 行程分享、导出、打印及多人协作。
- 酒店、机票、火车票、餐厅预订或支付。
- 实时天气、实时客流、实时票务库存和动态价格。
- 多城市联程、跨城市交通和境外路线规划。
- AI 自动补写第三方接口未提供的开放时间、票价或路线事实。

## 3. 产品原则

1. **可执行优先**：交通耗时、景点游玩时长和已知开放时间必须计入日程。
2. **用户选择优先**：只规划当前已选择的景点，不擅自添加其他景点。
3. **事实与建议分离**：景点和路线事实来自外部 API；AI 只负责规划、排序和建议游玩时长。
4. **缺失即标注**：数据缺失时展示规定文案，不推测、不补全。
5. **失败可恢复**：调用失败后保留用户已输入及已选择的内容，并允许重试。

## 4. 用户流程

1. 用户进入首页。
2. 用户填写目的地、开始日期、结束日期、每日可游玩开始时间、每日可游玩结束时间和出行方式。
3. 用户点击“查询景点”。
4. 系统通过服务端调用阿里云景点 API，展示目的地相关景点。
5. 用户勾选想去的景点。
6. 用户点击“生成行程”。
7. 系统校验输入和景点选择，并通过高德地图 API 查询所需路线及交通耗时。
8. 系统将用户时间约束、已选景点信息及路线数据发送给 DeepSeek。
9. 系统校验 DeepSeek 返回的结构化结果。
10. 页面按天展示行程、交通路线、景点简介、开放时间和参考票价。
11. 用户增加或删除景点后点击“重新生成”，系统基于最新选择重复步骤 7—10。

若任一外部服务调用失败，页面应指出失败环节、保留当前状态并提供重试入口。

## 5. 功能需求

需求优先级定义：P0 为 MVP 上线必需，P1 为可在 MVP 后补充。

### 5.1 旅行条件输入区

| 编号 | 优先级 | 功能 | 详细要求 |
| --- | --- | --- | --- |
| FR-01 | P0 | 输入目的地 | 必填，支持输入城市或区县名称；去除首尾空格后不能为空。 |
| FR-02 | P0 | 选择游玩日期 | 必填开始日期和结束日期；结束日期不得早于开始日期。 |
| FR-03 | P0 | 设置每日可游玩时间 | 必填每日开始时间和结束时间；结束时间必须晚于开始时间。首版所有日期使用同一时间范围。 |
| FR-04 | P0 | 选择出行方式 | 必填且单选：公共交通、自驾、两种均可。 |
| FR-05 | P0 | 查询景点 | 输入校验通过后发起查询；请求期间按钮禁用并展示加载状态，避免重复提交。 |
| FR-06 | P0 | 表单校验提示 | 在对应字段附近展示明确、可操作的中文错误提示。 |

### 5.2 景点查询与选择区

| 编号 | 优先级 | 功能 | 详细要求 |
| --- | --- | --- | --- |
| FR-07 | P0 | 展示景点列表 | 每个景点至少展示名称、简介、开放时间、参考票价和选择状态。 |
| FR-08 | P0 | 选择景点 | 支持单个勾选、取消；已选数量应持续可见。 |
| FR-09 | P0 | 数据缺失提示 | 开放时间缺失时显示“暂无数据”；票价缺失或准确性不足时显示“请以景点官方信息为准”。 |
| FR-10 | P0 | 空结果状态 | 未查询到景点时提示用户更换或细化目的地，不展示虚构景点。 |
| FR-11 | P0 | 查询失败重试 | 显示景点服务查询失败及重试按钮，保留旅行条件。 |
| FR-12 | P1 | 景点列表筛选 | 可按景点名称在已加载结果中筛选；不作为 MVP 上线阻塞项。 |

### 5.3 行程生成

| 编号 | 优先级 | 功能 | 详细要求 |
| --- | --- | --- | --- |
| FR-13 | P0 | 生成前校验 | 未选择任何景点时禁止生成，并提示“请至少选择一个景点”。 |
| FR-14 | P0 | 查询路线 | 根据已选景点坐标查询景点之间的路线、交通耗时和距离。 |
| FR-15 | P0 | 公共交通模式 | 请求高德公共交通路线，并将有效路线交给规划服务。 |
| FR-16 | P0 | 自驾模式 | 请求高德驾车路线，并将有效路线交给规划服务。 |
| FR-17 | P0 | 两种均可模式 | 对规划所需的每一段路线同时查询公共交通和自驾方案，由 DeepSeek 结合时间可行性选择；结果必须标注最终选用方式。 |
| FR-18 | P0 | AI 规划 | 将旅行条件、已选景点事实数据和路线数据发送给 DeepSeek，返回按日、按时间排序的结构化行程。 |
| FR-19 | P0 | 生成状态 | 路线查询和 AI 规划期间显示当前阶段；禁用重复生成操作。 |
| FR-20 | P0 | 结果校验 | 服务端校验 AI 返回的数据结构、日期范围、景点集合及必要字段；不合格结果不得直接展示。 |

### 5.4 行程结果区

| 编号 | 优先级 | 功能 | 详细要求 |
| --- | --- | --- | --- |
| FR-21 | P0 | 按日展示 | 以日期为分组展示每天的行程，日期按时间先后排列。 |
| FR-22 | P0 | 展示时间安排 | 每个项目展示建议到达时间、游玩开始/结束时间及建议游玩时长。 |
| FR-23 | P0 | 展示交通信息 | 景点之间展示交通方式、预计耗时、距离和路线摘要；首个景点没有出发地信息时无需伪造交通段。 |
| FR-24 | P0 | 展示景点信息 | 展示景点名称、简介、开放时间、参考票价及数据缺失提示。 |
| FR-25 | P0 | 展示未安排景点 | 因时间不足、闭馆或路线不可达而未安排的景点应单独列出，并说明原因。 |
| FR-26 | P0 | 风险提示 | 对开放时间未知、票价不确定或路线不可达等情况给出醒目标记。 |

### 5.5 行程调整与重新生成

| 编号 | 优先级 | 功能 | 详细要求 |
| --- | --- | --- | --- |
| FR-27 | P0 | 删除景点 | 用户可从当前已选列表中删除景点。 |
| FR-28 | P0 | 增加景点 | 用户可返回景点列表增加景点，原旅行条件和其他选择保持不变。 |
| FR-29 | P0 | 标记结果已过期 | 已生成行程后变更景点或旅行条件，应提示现有结果需重新生成，不得将旧结果伪装为最新结果。 |
| FR-30 | P0 | 重新生成 | 基于当前输入和最新景点选择重新查询必要路线并生成完整新行程。 |
| FR-31 | P0 | 保留状态 | 重新生成失败时保留用户输入、已选景点和上一份成功结果，并明确上一份结果已经过期。 |

## 6. 页面与交互要求

### 6.1 页面结构

MVP 使用单页分阶段流程，页面从上到下包含：

1. 页头与产品说明。
2. 旅行条件输入区。
3. 景点查询与选择区。
4. 已选景点摘要及“生成行程”按钮。
5. 行程生成状态区。
6. 行程结果区与未安排景点区。

### 6.2 响应式要求

- 桌面端采用适合横向浏览的内容容器，景点可使用多列卡片布局。
- 移动端使用单列布局，主要按钮应易于触控，关键信息不依赖横向滚动。
- 输入控件、错误提示、加载状态和操作按钮在常见手机与桌面尺寸下均应完整可见。

### 6.3 状态反馈

- 初始状态：仅展示条件输入，不显示空白行程卡片。
- 加载状态：明确区分“正在查询景点”“正在查询路线”“正在生成行程”。
- 成功状态：展示数据更新时间或“本次规划结果”标识。
- 空状态：提供原因和下一步建议。
- 失败状态：展示失败环节、简明原因及重试按钮，不暴露第三方原始错误或密钥。

## 7. 业务规则

### 7.1 输入与日期规则

1. 目的地、开始日期、结束日期、每日开始时间、每日结束时间和出行方式均为必填项。
2. 结束日期必须大于或等于开始日期。
3. 每日结束时间必须晚于每日开始时间。
4. MVP 将用户输入的日期和时间视为目的地当地时间，不进行跨时区行程计算。

### 7.2 行程安排规则

1. 只能安排用户当前选中的景点，每个景点最多安排一次。
2. 行程不得超出用户设置的每日可游玩时间。
3. 相邻景点之间必须预留所选路线的预计交通耗时。
4. 已知开放时间的景点不得被安排在闭馆时间之外；开放时间未知时可以安排，但必须展示“开放时间暂无数据，请出行前核实”的风险提示。
5. 建议游玩时长可在供应商数据缺失时由 DeepSeek生成，但必须标注为“AI 规划建议”，不得表述为官方推荐。
6. 当全部景点无法在时间范围内合理安排时，应优先保证已安排行程可执行，并将剩余景点放入“未安排景点”，不得通过压缩交通时间或制造重叠强行排入。
7. 当某一景点路线不可达时，不得伪造路线；应将其标记为未安排或提示用户更换交通方式。
8. AI 返回的路线方式和耗时必须来自所提供的有效路线选项，不得自行创造新的事实数据。

### 7.3 两种均可规则

1. 系统为规划涉及的每段景点间路线分别查询公共交通和自驾方案。
2. 若两种方案均有效，DeepSeek 可按耗时、当天可用时间和整体路线连贯性选择其中一种。
3. 若仅一种方案有效，只能选择有效方案。
4. 若两种方案均不可用，该路线段视为不可达。
5. 结果必须逐段展示实际选用的交通方式，不得只显示“两种均可”。

### 7.4 数据真实性规则

1. 景点名称、简介、坐标、开放时间和参考票价以景点 API 返回值为事实来源。
2. 距离、交通方式、路线摘要和预计交通耗时以高德地图 API 返回值为事实来源。
3. DeepSeek 负责行程分组、顺序、时间分配、交通方案选择和建议游玩时长，不作为开放时间、票价或路线事实来源。
4. 外部数据缺失时使用固定占位文案，不允许 DeepSeek 或前端根据常识补全。
5. 参考票价存在但可能变化时，应同时提示“价格仅供参考，请以景点官方信息为准”。

## 8. 异常处理与降级策略

| 场景 | 页面行为 | 状态保留 |
| --- | --- | --- |
| 景点 API 超时或失败 | 提示“景点信息查询失败，请稍后重试”，提供重试 | 保留旅行条件 |
| 景点 API 无结果 | 提示未找到相关景点，建议检查目的地 | 保留旅行条件 |
| 高德 API 部分路线失败 | 对失败路线重试一次；仍失败则标记不可达并继续判断是否可规划 | 保留条件与已选景点 |
| 高德 API 整体失败 | 提示“路线查询失败”，停止 AI 规划并提供重试 | 保留条件与已选景点 |
| DeepSeek 超时或失败 | 提示“行程生成失败”，提供重新生成 | 保留条件、已选景点和路线查询状态 |
| DeepSeek 返回非预期结构 | 服务端拒绝结果，可自动重试一次；仍失败则提示生成失败 | 保留全部用户状态 |
| 外部接口限流 | 提示服务繁忙，避免连续自动重试，允许用户稍后手动重试 | 保留全部用户状态 |
| 用户修改条件或景点 | 标记当前结果已过期，提示重新生成 | 保留旧结果供参考，直至新结果成功 |

任何失败场景均不得自动切换为未标注的模拟数据或 AI 编造数据。

## 9. 数据模型

以下模型用于描述前后端统一语义，具体实现可使用 TypeScript 类型并通过运行时 Schema 进行服务端校验。

### 9.1 TripPreferences

```ts
type TransportPreference = "transit" | "driving" | "either";

interface TripPreferences {
  destination: string;
  startDate: string; // YYYY-MM-DD
  endDate: string; // YYYY-MM-DD
  dailyStartTime: string; // HH:mm
  dailyEndTime: string; // HH:mm
  transportPreference: TransportPreference;
}
```

### 9.2 ScenicSpot

```ts
type DataAvailability = "available" | "missing" | "uncertain";

interface ScenicSpot {
  id: string;
  name: string;
  location: {
    longitude: number;
    latitude: number;
  };
  description: string | null;
  openingHours: string | null;
  openingHoursStatus: DataAvailability;
  referencePrice: string | null;
  priceStatus: DataAvailability;
  source: "aliyun-scenic-api";
}
```

### 9.3 RouteOption

```ts
type RouteMode = "transit" | "driving";

interface RouteOption {
  originSpotId: string;
  destinationSpotId: string;
  mode: RouteMode;
  durationMinutes: number | null;
  distanceMeters: number | null;
  summary: string | null;
  reachable: boolean;
  source: "amap-api";
}
```

### 9.4 Itinerary

```ts
interface ItineraryItem {
  spotId: string;
  arrivalTime: string; // HH:mm
  visitStartTime: string; // HH:mm
  visitEndTime: string; // HH:mm
  suggestedVisitMinutes: number;
  visitDurationSource: "provider" | "ai-suggestion";
  routeFromPrevious: RouteOption | null;
}

interface ItineraryDay {
  date: string; // YYYY-MM-DD
  items: ItineraryItem[];
}

interface UnscheduledSpot {
  spotId: string;
  reason: "insufficient_time" | "closed" | "unreachable" | "invalid_data";
  message: string;
}

interface ItineraryResult {
  days: ItineraryDay[];
  unscheduledSpots: UnscheduledSpot[];
  warnings: string[];
}
```

## 10. 系统与接口约定

### 10.1 技术边界

- 使用 Next.js App Router 和 TypeScript。
- 页面交互状态仅保存在当前浏览器页面状态中，不写入数据库或浏览器持久化存储。
- 所有第三方 API 均通过 Next.js 服务端 Route Handlers 调用。
- 前端不得直接请求需要私密 Key 的第三方接口。

### 10.2 建议的内部接口

| 接口 | 方法 | 用途 | 主要输入 | 主要输出 |
| --- | --- | --- | --- | --- |
| `/api/scenic-spots` | POST | 查询目的地景点 | `destination` | `ScenicSpot[]` |
| `/api/itinerary` | POST | 查询路线并生成行程 | `TripPreferences`、已选 `spotIds` 或完整已选景点 | `ItineraryResult` |

`/api/itinerary` 应由服务端完成路线查询、DeepSeek 调用和结果校验，避免前端获得第三方 Key 或信任未经校验的模型输出。

### 10.3 阿里云景点 API 适配

- 通过独立适配层将实际购买的阿里云或阿里云市场景点 API 响应映射为 `ScenicSpot`。
- PRD 不预设具体供应商路径、鉴权头和原始字段名，开发前应依据已购买产品的官方文档完成映射。
- 原始字段不存在时应映射为 `null` 并设置对应的数据状态，不得在适配层推测内容。
- 若实际接口还需要 AppCode、AppSecret 或 Host，应分别配置环境变量，不得写死在代码中。

### 10.4 高德地图 API 适配

- 使用已选景点的坐标查询路线。
- 公交模式调用公共交通路线能力，自驾模式调用驾车路线能力。
- 路线适配层统一输出 `RouteOption`，并将供应商无结果或错误转换为 `reachable: false`。
- 应控制路线查询次数并处理高德配额、并发和限流错误。

### 10.5 DeepSeek 规划约束

- 发送给 DeepSeek 的内容只包含规划所需的用户条件、已选景点数据和路线选项。
- Prompt 必须明确：不得添加未选择景点，不得修改开放时间、票价、路线方式、路线距离或路线耗时。
- 要求返回符合 `ItineraryResult` 的 JSON，不接受 Markdown 行程作为最终接口结果。
- 服务端必须进行运行时结构校验和业务校验，至少验证：日期范围、景点 ID、时间格式、时间先后、每日边界、景点唯一性及路线引用有效性。
- 校验失败可自动重新请求一次并附带格式纠正要求；第二次仍失败则返回可重试错误。

### 10.6 环境变量

至少配置：

```dotenv
DEEPSEEK_API_KEY=
AMAP_API_KEY=
ALIYUN_SCENIC_API_KEY=
```

根据实际服务可增加：

```dotenv
DEEPSEEK_API_BASE_URL=
ALIYUN_SCENIC_API_HOST=
ALIYUN_SCENIC_API_APPCODE=
```

- 真实密钥只能存在于本地 `.env.local` 或部署平台的加密环境变量中。
- 仅提供不含真实值的 `.env.example`。
- 这些变量不得使用 `NEXT_PUBLIC_` 前缀。
- 服务端日志、错误响应和监控信息不得包含密钥、完整鉴权头或完整第三方请求地址中的敏感参数。

## 11. 非功能需求

### 11.1 性能与稳定性

- 用户提交后应立即显示阶段性加载反馈。
- 正常网络下，景点查询目标响应时间不超过 5 秒；超过目标时间仍须保持加载或给出超时提示。
- 行程生成目标在 30 秒内完成；超过服务端设定超时时间后应返回可重试错误。
- 前端应防止重复点击造成并发重复请求。
- 第三方调用应设置超时，并对适合重试的临时错误进行有限次数重试。

### 11.2 安全

- API Key 仅在服务端读取和使用。
- 对所有用户输入进行类型、长度和格式校验。
- 不直接向用户返回第三方服务的原始异常堆栈。
- 对 DeepSeek 输出按不可信输入处理，必须验证后再渲染。
- 页面渲染景点简介和路线摘要时应避免注入风险。

### 11.3 可访问性与兼容性

- 表单控件应有关联标签，错误提示不能只依赖颜色表达。
- 键盘可完成输入、景点选择、生成和重试操作。
- 加载状态和错误信息应可被辅助技术识别。
- 支持当前主流 Chrome、Edge、Safari 移动端和桌面端版本。

### 11.4 数据与隐私

- MVP 不持久化用户行程，刷新或关闭页面后允许数据丢失。
- 不主动收集姓名、联系方式、证件或支付信息。
- 日志仅记录排障所需的请求状态、耗时和匿名错误信息。

## 12. 埋点与产品指标

MVP 不建设复杂分析系统；如项目已有匿名埋点能力，可记录以下不含个人敏感信息的事件：

- 景点查询发起、成功、失败。
- 行程生成发起、成功、失败。
- 重新生成次数。
- 未安排景点数量和失败类型。

核心验证指标：

- 端到端行程生成成功率。
- 景点查询和行程生成失败率。
- 用户首次成功生成行程所需时间。
- 生成后进行景点调整并重新生成的比例。

## 13. 测试与验收标准

### 13.1 功能验收

1. 用户可完成单日和多日行程的完整流程。
2. 公共交通、自驾和“两种均可”三种模式均可生成带明确交通方式的结果。
3. 用户增删景点后重新生成，结果只包含当前已选景点。
4. 页面刷新后状态清空，系统不依赖数据库或本地持久化恢复结果。

### 13.2 校验验收

1. 结束日期早于开始日期时阻止查询或生成。
2. 每日结束时间不晚于开始时间时阻止查询或生成。
3. 目的地为空时阻止查询。
4. 未选择景点时阻止生成。
5. 错误提示与错误字段对应，修正后可以继续操作。

### 13.3 规划可执行性验收

1. 景点在日期间合理分配，同一景点不重复安排。
2. 相邻景点交通耗时计入时间线，项目之间不存在时间重叠。
3. 每天首项不早于每日开始时间，末项不晚于每日结束时间。
4. 已知开放时间的景点不安排在闭馆区间。
5. 景点过多或时间不足时产生“未安排景点”，不强行压缩或重叠。
6. 路线不可达时不生成虚构路线，并展示可理解的原因。

### 13.4 数据真实性验收

1. 开放时间缺失时显示“暂无数据”。
2. 票价缺失或不确定时显示“请以景点官方信息为准”。
3. AI 建议游玩时长明确标注为建议。
4. DeepSeek 返回的开放时间、票价或非候选路线数据不得覆盖供应商事实数据。
5. 页面不得出现未由用户选择的 AI 新增景点。

### 13.5 异常恢复验收

1. 景点、路线和 DeepSeek API 分别发生超时、限流、无结果或响应格式异常时，页面指出对应失败环节。
2. 失败后用户输入和已选景点保持不变，并可重试。
3. 重新生成失败时，上一份成功结果仍可查看但被明确标记为已过期。
4. 失败时不自动展示未标注的模拟内容。

### 13.6 安全验收

1. 前端源码、浏览器网络响应和可下载资源中不存在任何第三方 API Key。
2. 仓库中不存在真实密钥，仅包含环境变量名称和示例文件。
3. 服务端错误响应和日志不暴露密钥、鉴权头或内部堆栈。
4. DeepSeek 的非 JSON、缺字段、未知景点 ID 和越界时间结果均被服务端拒绝。

## 14. MVP 完成定义

同时满足以下条件时，可认为 MVP 达到完成标准：

1. 用户能够从输入旅行条件开始，完成景点查询、选择、路线查询、AI 规划和行程展示的端到端流程。
2. 输出按天展示景点顺序、时间安排、交通方式、交通耗时、景点简介、开放时间和参考票价。
3. 生成结果没有明显时间重叠，交通耗时已计入，且不超出每日可用时间。
4. 外部数据缺失时使用规定提示，未发生 AI 补写事实信息。
5. 用户增删景点后可以重新生成完整行程。
6. 外部服务失败时可以恢复和重试，用户当前页面状态不丢失。
7. 所有 API Key 均通过服务端环境变量管理，未暴露给浏览器或提交到代码仓库。

## 15. 已知依赖与风险

- 阿里云景点 API 的具体产品、字段完整度、调用额度和鉴权方式需在开发前确认；不同产品必须通过适配层统一数据结构。
- 景点开放时间和票价可能缺失或过期，页面必须持续展示数据来源限制提示。
- 高德公共交通路线可能受城市、时间或服务范围限制，需正确处理无路线结果。
- 多景点路线组合数量可能快速增长，开发时需控制查询范围、并发数和第三方调用成本。
- 大模型输出存在不稳定性，结构校验和业务校验是上线前置条件，不能只依赖 Prompt。
- MVP 未纳入用户当前位置或酒店位置，因此首个景点前、最后一个景点后的交通不在规划范围内。

## 16. 默认假设

- 产品面向普通自由行用户，以单个目的地城市或区县为一次规划范围。
- 用户输入的每日可游玩时段适用于行程中的每一天。
- 景点建议游玩时长在供应商缺失时可由 DeepSeek生成，并标注为“AI 规划建议”。
- 当前页面状态是唯一数据来源，页面刷新或关闭后允许丢失。
- 首版不提供账号、数据库、历史记录、协作、分享、导出、酒店或交通票务预订。

